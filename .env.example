# Moorcheh API key — in production this MUST move to a backend proxy
MOORCHEH_API_KEY=your-moorcheh-api-key-here

# Cloudflare Worker URL (deployed proxy for LLM calls)
WORKER_URL=https://flare-health-proxy.your-subdomain.workers.dev
