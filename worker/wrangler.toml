name = "flare-health-llm-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# Optional: HF_API_KEY (defaults to "test" if not set)
# Set via: wrangler secret put HF_API_KEY
# For this endpoint, "test" is sufficient
